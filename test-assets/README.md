# test-assets

Sample images for manually trying out render settings (dithering, character
sets, brightness/levels, edge detection) against real photos, separate from
the synthetic fixtures used by the automated test suite in `tests/`.

Not referenced by any code or test - drop an image here, commit it, and it's
available to pull and load into the app.
