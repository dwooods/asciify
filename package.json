{
  "name": "asciify",
  "private": true,
  "description": "Convert images into Unicode braille dot-matrix text art in the browser.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
